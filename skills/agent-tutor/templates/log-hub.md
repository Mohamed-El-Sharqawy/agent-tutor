---
type: log
subject: {{subject}}
date: {{DATE}}
tags: [learning, log]
---

# 📅 {{DATE}} — session log

> [!info] HTML log mode
> Today's entries live in `{{DATE}}.html` — this stub is only a signpost so the day shows up in Obsidian, which hides `.html` files unless the **HTML Reader** plugin is installed. Open the log with that plugin, or in any browser.

**Open today's log:** [{{DATE}}.html]({{DATE}}.html)

**Entries:**
- {{HH:MM}} — {{short title}} ({{type}})
<!-- Markdown signpost (html log mode only): the FIRST .html entry of the day creates this stub next to the html file; every later entry appends one bullet under Entries. Signpost only — never a parallel log, never parsed for state; content lives in the html file and is never rewritten. -->
